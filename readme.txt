This project focuses on learning the next.js framework.
this project was also created without the use of 
create-react-app.

to install,
run "npm install" to install dependencies

to run the app 
"npm start"
